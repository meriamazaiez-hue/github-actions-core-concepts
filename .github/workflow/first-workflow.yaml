name: First workflow
on: push
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: checkout repository
              uses: actions/checkout@v4

            - name: Test job Welcom messages
              run: echo "Executing the test job"
                   cat README.md